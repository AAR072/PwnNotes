# Port Forwarding

```portfwd localhost:port->target:port```

This lets us see what is on target:port at localhost:port

# Penelope SSH

```penelope ssh user@target```

You can put `--` after the command to add whatever ssh parameters you want.

# Standard privesc linux:

Command + f12 puts us into penelope shell

`run lse`
`run peass_ng`
`run linuxexploitsuggester`

